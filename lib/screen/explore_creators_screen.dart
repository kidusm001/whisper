import 'package:flutter/material.dart';

class ExploreCreatorsScreen extends StatelessWidget {
  const ExploreCreatorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Explore Creators Screen'),
    );
  }
}
